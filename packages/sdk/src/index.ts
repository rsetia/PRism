/**
 * Public entry point for @rsetia/agent-graph.
 *
 * Placeholder export proving the build pipeline; the real surface
 * (parseGraph, compileGraph, createEngine, ...) lands section by section.
 */
export const SDK_VERSION = "0.1.0-alpha.0";
